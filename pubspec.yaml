name: fitplan_creator
description: A fitness planner application
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # State Management & Navigation
  flutter_riverpod: ^2.4.9
  go_router: ^13.0.0

  # UI (оставил только необходимые для MVP)
  url_launcher: ^6.2.1
  shared_preferences: ^2.2.2
  # Убрал временно: cached_network_image, fluttertoast, lottie, flutter_svg - добавим позже

  # Backend
  supabase_flutter: ^2.5.6
  
  # Utils
  intl: ^0.19.0
  uuid: ^4.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  # Убрал временно: build_runner, riverpod_generator, riverpod_annotation - не используем пока

flutter:
  uses-material-design: true
  generate: true
  
  assets:
    - assets/exercises/
    # Убрал временно несуществующие папки: assets/images/, assets/lottie/, assets/data/