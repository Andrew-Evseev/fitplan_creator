# ⚡ Быстрое решение проблемы с npm

## Проблема

Вы получили ошибку:
```
npm error 404 '@supabase-community/mcp-server-supabase@latest' is not in this registry.
```

## ✅ Решение (за 2 минуты)

### Для self-hosted Supabase:

**MCP endpoint ВСТРОЕН в Supabase!** Вам не нужно устанавливать npm пакет.

### Что сделать:

1. **Включите MCP endpoint в Supabase:**
   ```bash
   cd /path/to/supabase  # ваша директория Supabase
   nano ./volumes/api/kong.yml
   ```
   
   Найдите секцию `name: mcp` и:
   - Закомментируйте или удалите `request-termination` плагин
   - Добавьте IP в секцию `allow` (например, `172.18.0.1`)

2. **Перезапустите Kong:**
   ```bash
   docker-compose restart kong
   ```

3. **Настройте SSH туннель (на вашем компьютере):**
   ```powershell
   ssh -N -L 3100:localhost:8000 root@ваш-ip-сервера
   ```

4. **Настройте Cursor через HTTP:**
   ```json
   {
     "mcpServers": {
       "supabase": {
         "type": "http",
         "url": "http://localhost:3100/mcp"
       }
     }
   }
   ```

5. **Готово!** Теперь можете использовать Cursor с базой данных.

---

## 📚 Подробная инструкция

См. файл `РЕШЕНИЕ_ПРОБЛЕМЫ.md` для подробных шагов.
