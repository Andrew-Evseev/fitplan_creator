# Быстрое решение проблемы с регистрацией

## Проблема
При регистрации пользователь не создается в таблице `profiles` в Supabase.

## Решение 1: Отключить подтверждение email (для тестирования)

1. Откройте Supabase Dashboard: http://YOUR_SERVER_IP:8000
2. Перейдите в **Authentication** → **Settings** → **Email Auth**
3. **Снимите галочку** "Enable email confirmations"
4. Сохраните изменения

Это позволит пользователям регистрироваться без подтверждения email.

## Решение 2: Проверить триггер

1. В Supabase Dashboard перейдите в **Database** → **SQL Editor**
2. Выполните скрипт из файла `supabase/migrations/004_check_trigger.sql`
3. Проверьте результаты:
   - Функция `handle_new_user()` должна существовать
   - Триггер `on_auth_user_created` должен существовать и быть привязан к `auth.users`

## Решение 3: Пересоздать триггер вручную

Если триггер не работает, выполните в SQL Editor:

```sql
-- Удалить старый триггер
DROP TRIGGER IF EXISTS on_auth_user_created ON auth.users;

-- Создать триггер заново
CREATE TRIGGER on_auth_user_created
    AFTER INSERT ON auth.users
    FOR EACH ROW
    EXECUTE FUNCTION public.handle_new_user();
```

## Решение 4: Проверить логи

1. В Supabase Dashboard перейдите в **Logs**
2. Фильтруйте по **Database**
3. Попробуйте зарегистрироваться
4. Ищите ошибки, связанные с `handle_new_user` или `profiles`

## Решение 5: Создать профиль вручную (временное решение)

Если триггер не работает, можно создать профиль вручную:

1. Зарегистрируйтесь в приложении
2. В Supabase Dashboard перейдите в **Database** → **Table Editor** → **auth.users**
3. Найдите последнего созданного пользователя (по email)
4. Скопируйте его `id` (UUID)
5. Выполните в SQL Editor:

```sql
-- Замените USER_ID_ЗДЕСЬ на реальный UUID пользователя
INSERT INTO public.profiles (id, name, created_at, updated_at)
VALUES (
    'USER_ID_ЗДЕСЬ',
    'Пользователь',
    now(),
    now()
);

INSERT INTO public.user_stats (user_id, updated_at)
VALUES ('USER_ID_ЗДЕСЬ', now());

INSERT INTO public.user_settings (user_id, updated_at)
VALUES ('USER_ID_ЗДЕСЬ', now());
```

## Проверка после исправления

1. Зарегистрируйтесь в приложении
2. Проверьте консоль Flutter - должны быть логи:
   - `📝 Попытка регистрации: ...`
   - `✅ Регистрация успешна: ...`
3. В Supabase Dashboard проверьте таблицу `profiles` - должен появиться новый профиль

## Если ничего не помогает

Выполните полную миграцию заново:
1. Откройте `supabase/migrations/000_apply_all_migrations.sql`
2. Скопируйте весь код
3. Вставьте в SQL Editor
4. Нажмите Run
