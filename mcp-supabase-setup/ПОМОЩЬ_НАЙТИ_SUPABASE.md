# 🔍 Помощь: Как найти папку Supabase на сервере

## Проблема

Команда `find / -name "docker-compose.yml" 2>/dev/null | grep supabase` ничего не нашла.

## Решение

### Шаг 1: Проверить, существует ли папка `/root/supabase`

Выполните на сервере:

```bash
ls -la /root/supabase
```

**Если папка существует:**
- Вы увидите список файлов
- Переходите к следующему шагу

**Если папка НЕ существует:**
- Вы увидите ошибку: `No such file or directory`
- Продолжайте поиск (см. Шаг 2)

### Шаг 2: Поиск в других местах

Выполните на сервере (по очереди):

```bash
# Поиск в корне
ls -la /root/ | grep supabase

# Поиск в /opt
ls -la /opt/ | grep supabase

# Поиск в /home
ls -la /home/ | grep supabase

# Поиск в текущей директории
ls -la | grep supabase
```

### Шаг 3: Поиск docker-compose.yml без фильтра

```bash
find / -name "docker-compose.yml" 2>/dev/null
```

Эта команда найдёт ВСЕ файлы `docker-compose.yml` на сервере, не только из Supabase.

**Результат может быть:**
```
/root/supabase/docker-compose.yml
/opt/supabase/docker-compose.yml
/path/to/another/docker-compose.yml
```

Попробуйте перейти в каждую найденную директорию и проверить, есть ли там Supabase:

```bash
cd /найденный/путь
cat docker-compose.yml | grep supabase
```

### Шаг 4: Поиск по процессам Docker

Если Supabase работает, можно найти через Docker:

```bash
# Посмотреть все контейнеры
docker ps -a | grep supabase

# Посмотреть информацию о контейнере (замените supabase-kong на имя вашего контейнера)
docker inspect supabase-kong | grep -i "com.docker.compose.project.working_dir"
```

### Шаг 5: Поиск в конфигурации Docker

```bash
# Найти где монтируются volumes Supabase
docker inspect supabase-kong 2>/dev/null | grep -i "source"
```

Это покажет, откуда монтируются файлы, что может указать на директорию Supabase.

---

## Альтернатива: Если Supabase установлен через Docker Compose нестандартно

Иногда Supabase может быть установлен не через стандартную установку. Проверьте:

1. **Docker Compose проекты:**
   ```bash
   docker ps --format "table {{.Names}}\t{{.Image}}" | grep supabase
   ```

2. **Docker сети:**
   ```bash
   docker network ls | grep supabase
   ```

3. **Файлы конфигурации:**
   ```bash
   find /root -name "*.env" 2>/dev/null | xargs grep -l "SUPABASE" 2>/dev/null
   ```

---

## Что делать дальше?

1. **Выполните команды выше** на сервере
2. **Скопируйте результаты** и покажите мне
3. **Я помогу найти** правильную директорию

Или, если вы точно знаете, что Supabase установлен, но не помните где:
- Проверьте историю команд: `history | grep supabase`
- Проверьте логи Docker: `docker logs supabase-kong 2>&1 | head -20`

---

## Если Supabase не установлен

Если Supabase действительно не установлен на сервере, нужно сначала его установить. Но по вашему сообщению похоже, что он уже установлен, просто нужно найти папку.
