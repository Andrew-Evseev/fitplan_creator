# 🔒 Безопасность и Конфигурация

## ⚠️ ВАЖНО: Секреты не должны быть в коде!

Все секретные данные (ключи, пароли) должны храниться в переменных окружения, а не в коде.

## Настройка переменных окружения

### Для локальной разработки:

1. Скопируйте `env.example` в `.env`:
   ```bash
   cp env.example .env
   ```

2. Заполните `.env` реальными значениями:
   ```env
   SUPABASE_URL=http://your-server:8000
   SUPABASE_ANON_KEY=your_anon_key_here
   ```

3. Запустите приложение с переменными окружения:
   ```bash
   flutter run --dart-define=SUPABASE_URL=your_url --dart-define=SUPABASE_ANON_KEY=your_key
   ```

### Для продакшена:

Используйте переменные окружения вашей платформы развертывания (Vercel, Netlify, etc.)

## Что делать, если секреты уже попали в Git:

1. **Немедленно ротируйте все ключи:**
   - Сгенерируйте новые ключи в Supabase Dashboard
   - Измените все пароли

2. **Удалите секреты из истории Git:**
   ```bash
   # Используйте git filter-repo (рекомендуется)
   git filter-repo --path lib/core/supabase/supabase_config.dart --invert-paths
   
   # Или создайте новый коммит, удаляющий секреты
   ```

3. **Обновите .gitignore:**
   - Убедитесь, что `.env` и другие файлы с секретами в `.gitignore`

## Проверка безопасности:

Перед каждым коммитом проверяйте:
```bash
# Поиск потенциальных секретов
grep -r "password\|secret\|key" --include="*.dart" --include="*.md" lib/
```
