# Устранение проблем с регистрацией

## Проблема: Регистрация не создает профиль в Supabase

### Шаг 1: Проверка триггера в Supabase

1. Откройте Supabase Dashboard: http://YOUR_SERVER_IP:8000
2. Перейдите в **Database** → **Triggers**
3. Найдите триггер `on_auth_user_created`
4. Убедитесь, что он:
   - Существует
   - Включен (Enabled)
   - Привязан к таблице `auth.users`
   - Выполняет функцию `handle_new_user()`

### Шаг 2: Проверка функции

1. В Supabase Dashboard перейдите в **Database** → **Functions**
2. Найдите функцию `handle_new_user()`
3. Убедитесь, что она существует и правильно настроена

### Шаг 3: Проверка настройки Email Confirmation

1. В Supabase Dashboard перейдите в **Authentication** → **Settings**
2. Найдите раздел **Email Auth**
3. Проверьте настройку **"Enable email confirmations"**
4. **Для тестирования рекомендуется отключить** подтверждение email:
   - Снимите галочку "Enable email confirmations"
   - Сохраните изменения

### Шаг 4: Проверка логов

1. В Supabase Dashboard перейдите в **Logs**
2. Фильтруйте по **Auth** и **Database**
3. Попробуйте зарегистрироваться снова
4. Проверьте логи на наличие ошибок

### Шаг 5: Проверка в Flutter консоли

При запуске приложения с `flutter run`, в консоли должны появиться логи:
- `🔐 Попытка регистрации: ...`
- `✅ Регистрация успешна: ...`
- Или `❌ Ошибка: ...`

### Шаг 6: Ручная проверка триггера

Выполните в SQL Editor:

```sql
-- Проверить существование триггера
SELECT 
    trigger_name, 
    event_manipulation, 
    event_object_table, 
    action_statement,
    action_timing
FROM information_schema.triggers 
WHERE trigger_name = 'on_auth_user_created';

-- Проверить существование функции
SELECT routine_name, routine_type
FROM information_schema.routines
WHERE routine_name = 'handle_new_user' 
  AND routine_schema = 'public';
```

### Шаг 7: Тестовая регистрация через SQL

Если триггер не работает, можно создать профиль вручную:

```sql
-- Найти последнего созданного пользователя
SELECT id, email, raw_user_meta_data 
FROM auth.users 
ORDER BY created_at DESC 
LIMIT 1;

-- Создать профиль вручную (замените USER_ID на реальный ID)
INSERT INTO public.profiles (id, name, created_at, updated_at)
VALUES (
    'USER_ID_ЗДЕСЬ',
    COALESCE((SELECT raw_user_meta_data->>'name' FROM auth.users WHERE id = 'USER_ID_ЗДЕСЬ'), 'Пользователь'),
    now(),
    now()
);
```

## Частые проблемы

### Проблема 1: Email confirmation включен
**Решение:** Отключите в Authentication → Settings → Email Auth

### Проблема 2: Триггер не создан
**Решение:** Примените миграцию `000_apply_all_migrations.sql` снова

### Проблема 3: Ошибки прав доступа
**Решение:** Убедитесь, что функция `handle_new_user()` имеет `SECURITY DEFINER`

### Проблема 4: Пользователь создается, но профиль нет
**Решение:** Проверьте логи Database на ошибки при выполнении триггера
