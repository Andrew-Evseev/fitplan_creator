# Установка Supabase MCP сервера для Cursor

## Шаг 1: Очистка старых настроек на сервере

**На сервере** выполните:

```bash
cd ~/docker_data/supabase/docker
nano ./volumes/api/kong.yml
```

1. Найдите и **удалите секцию `mcp-api`** (строки примерно 229-243):
   ```yaml
   ## MCP endpoint
   - name: mcp-api
     _comment: 'MCP: /api/mcp -> http://studio:3000/api/mcp'
     url: http://studio:3000/api/mcp
     routes:
       - name: mcp-api-route
         strip_path: true
         paths:
           - /mcp
     plugins:
       - name: cors
       - name: basic-auth
         config:
           hide_credentials: true
   ```

2. Сохраните файл (Ctrl+X, Y, Enter)

3. Перезапустите Kong:
   ```bash
   docker compose restart kong
   ```

**Готово!** Старые настройки удалены.

---

## Шаг 2: Установка Node.js (если не установлен)

**На вашем компьютере (Windows):**

1. Проверьте, установлен ли Node.js:
   ```powershell
   node --version
   ```

2. Если команда не работает, скачайте и установите Node.js:
   - Перейдите на https://nodejs.org/
   - Скачайте LTS версию (рекомендуется)
   - Установите с настройками по умолчанию

3. Если `npm --version` показывает ошибку "выполнение сценариев отключено":
   
   **Решение:** Включите выполнение скриптов в PowerShell:
   ```powershell
   # Запустите PowerShell от имени администратора (правой кнопкой → "Запуск от имени администратора")
   Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
   ```
   
   На вопрос "Вы действительно хотите выполнить это действие?" введите `Y` и нажмите Enter.
   
   **Что это делает:** Разрешает выполнение локальных скриптов, но требует подпись для скачанных из интернета (безопасно).

4. После установки/настройки перезапустите PowerShell и проверьте снова:
   ```powershell
   node --version
   npm --version
   ```

---

## Шаг 3: Установка MCP сервера

**На вашем компьютере:**

1. **Выберите папку БЕЗ ПРОБЕЛОВ** для установки, например:
   ```
   C:\MCP\supabase-mcp-cursor
   ```

2. Создайте папку и перейдите в неё:
   ```powershell
   mkdir C:\MCP
   cd C:\MCP
   ```

3. Клонируйте репозиторий:
   ```powershell
   git clone https://github.com/SlippyDong/supabase-mcp-cursor.git
   ```

4. **Важно:** Перейдите в папку проекта:
   ```powershell
   cd supabase-mcp-cursor
   ```
   
   ⚠️ **В PowerShell команды нужно выполнять отдельно!** Не объединяйте `cd` и `npm install` в одну строку.

5. Установите зависимости:
   ```powershell
   npm install
   ```

5. Соберите проект:
   ```powershell
   npm run build
   ```

---

## Шаг 4: Настройка переменных окружения

**На вашем компьютере:**

1. В папке `C:\MCP\supabase-mcp-cursor` создайте файл `.env`:
   ```powershell
   notepad .env
   ```

2. Добавьте следующие строки:
   ```
   SUPABASE_URL=https://ваш-домен.supabase.co
   SUPABASE_KEY=ваш-service-role-key
   SUPABASE_ACCESS_TOKEN=ваш-access-token
   ```

   **Где найти эти данные:**
   - **SUPABASE_URL**: URL вашего Supabase проекта (self-hosted или cloud)
     - Если self-hosted: `http://ваш-ip:8000` или ваш домен
     - Если cloud: `https://xxxxx.supabase.co`
   
   - **SUPABASE_KEY**: Service Role Key (секретный ключ)
     - На сервере: `cat ~/docker_data/supabase/docker/.env | grep SERVICE_ROLE`
     - Или в Supabase Dashboard → Settings → API → Service Role Key
   
   - **SUPABASE_ACCESS_TOKEN**: Временно можно использовать Service Role Key (пока не реализовано в этом форке)

3. Сохраните файл (Ctrl+S) и закройте.

---

## Шаг 5: Тестирование MCP сервера

**На вашем компьютере:**

1. Проверьте, что сервер запускается:
   ```powershell
   node build\start-server.js
   ```

2. Вы должны увидеть:
   ```
   Cursor Tools MCP Server running on stdio
   ```

3. Нажмите `Ctrl+C` чтобы остановить.

---

## Шаг 6: Подключение к Cursor

**В Cursor IDE:**

1. Откройте настройки: **File** → **Preferences** → **Cursor Settings** (или `Ctrl+,`)

2. Перейдите в раздел: **Features** → **MCP Servers**

3. Нажмите **"Add MCP Server"**

4. Заполните форму:
   - **Name**: `supabase` (или любое другое имя)
   - **Type**: выберите **"Command"** (НЕ HTTP!)
   - **Command**: 
     ```
     node.exe C:\MCP\supabase-mcp-cursor\build\start-server.js
     ```
     **⚠️ ВАЖНО:** Путь должен быть БЕЗ ПРОБЕЛОВ!

5. Нажмите **"Save"** или **"OK"**

6. Подождите 10-15 секунд. Статус подключения должен стать зелёным (🟢)

7. Если всё работает, вы увидите список доступных инструментов (tools):
   - `create_record`
   - `read_records`
   - `update_record`
   - `delete_record`
   - `upload_file`
   - `download_file`
   - `invoke_function`
   - `list_projects`

---

## Шаг 7: Проверка работы

**В Cursor:**

1. Откройте чат с AI (Ctrl+L)

2. Попробуйте команду:
   ```
   Покажи мне список таблиц в базе данных Supabase
   ```

3. AI должен использовать MCP инструменты для работы с базой данных.

---

## Решение проблем

### Проблема: "No tools found" или сервер не запускается

**Решения:**
- ✅ Проверьте, что путь к серверу НЕ содержит пробелов
- ✅ Убедитесь, что Node.js установлен: `node --version`
- ✅ Проверьте, что файл `.env` существует и заполнен правильно
- ✅ Попробуйте запустить вручную: `node build\start-server.js`

### Проблема: Сервер не подключается к Supabase

**Решения:**
- ✅ Проверьте `SUPABASE_URL` в `.env` - он должен быть доступен с вашего компьютера
- ✅ Если self-hosted, убедитесь, что порты открыты или используйте SSH туннель
- ✅ Проверьте `SUPABASE_KEY` - должен быть правильный Service Role Key

### Проблема: Cursor показывает ошибку подключения

**Решения:**
- ✅ Закройте и перезапустите Cursor
- ✅ Убейте процессы Node.js в Task Manager (если сервер завис)
- ✅ Удалите и заново добавьте MCP сервер в настройках Cursor

---

## Готово! 🎉

Теперь вы можете использовать Cursor для работы с Supabase базой данных через естественный язык!

**Примеры команд:**
- "Создай таблицу users с полями id, name, email"
- "Покажи все записи из таблицы exercises"
- "Добавь новую запись в таблицу plans"
- "Обнови запись с id=1 в таблице users"
