# Настройка MCP сервера для начинающих разработчиков

## ⚠️ ВАЖНО: Начните отсюда!

**Для self-hosted Supabase MCP endpoint ВСТРОЕН в Supabase!** 
- ❌ НЕ нужно устанавливать npm пакет
- ✅ Нужно только включить MCP endpoint в конфигурации Supabase
- ✅ Использовать HTTP подключение в Cursor

---

## Что это такое? (Объяснение простыми словами)

**Метафора:** Представьте, что Cursor - это ваш личный помощник. Сейчас он может помочь только с кодом (например, "напиши функцию"). После настройки он сможет помочь и с базой данных (например, "создай таблицу пользователей").

**Что такое MCP сервер?**
- Это специальная программа, которая работает в вашем Supabase
- Она "переводит" ваши вопросы для Cursor на язык баз данных
- После настройки вы сможете говорить с Cursor о базе данных на простом языке

**Пример:**
- ❌ **До настройки:** "Напиши SQL запрос для создания таблицы" → Cursor может только показать пример кода
- ✅ **После настройки:** "Создай таблицу users" → Cursor создаст таблицу в вашей базе данных!

---

## Что вам нужно? (Чеклист)

### Обязательно:
- [ ] Supabase установлен на сервере timeweb.cloud (self-hosted)
- [ ] IP адрес или домен вашего сервера
- [ ] Логин и пароль для входа на сервер (обычно `root` + пароль)
- [ ] Доступ к папке Supabase на сервере (где находится `docker-compose.yml`)

### Желательно, но не обязательно:
- [ ] Базовое понимание командной строки

**Если чего-то нет - не страшно!** Я объясню, как это получить на каждом шаге.

---

## Пошаговая инструкция (очень подробная)

### 📋 ШАГ 1: Собрать информацию о вашем Supabase

**Зачем:** Нам нужна информация о вашем сервере и Supabase.

**Что делать:**

#### 1.1 Найти адрес вашего сервера

1. Войдите в панель управления timeweb.cloud
2. Найдите ваш сервер в списке
3. Скопируйте IP адрес (например, `123.45.67.89`) или домен
4. Запишите в блокнот: **IP адрес сервера:** _______________

#### 1.2 Найти директорию Supabase на сервере

1. Войдите на сервер через SSH (см. Шаг 2 ниже)
2. Найдите папку, где установлен Supabase (обычно там есть `docker-compose.yml`)
3. Запишите полный путь: **Путь к Supabase:** _______________

**Примеры путей:**
- `/root/supabase`
- `/opt/supabase`
- `/home/user/supabase`

**Как найти:**
```bash
# На сервере выполните
find / -name "docker-compose.yml" 2>/dev/null | grep supabase
```

**Готово!** Информация собрана, можно переходить к следующему шагу.

---

### 🔐 ШАГ 2: Подключиться к серверу (SSH)

**Зачем:** Чтобы настроить MCP endpoint в Supabase.

**Что такое SSH:** Это способ войти на удаленный компьютер через интернет, как будто вы сидите перед ним.

**Как это сделать на Windows:**

#### Способ A: PowerShell (встроен в Windows)

1. **Откройте PowerShell:**
   - Нажмите `Win + X` (клавиша Windows + X)
   - Выберите "Windows PowerShell" или "Терминал"

2. **Подключитесь к серверу:**
   Введите команду (замените на ваш IP адрес):
   ```
   ssh root@123.45.67.89
   ```
   
   **Замените:** `123.45.67.89` на ваш IP адрес

3. **Введите пароль:**
   - После команды система попросит пароль
   - Введите пароль (он не будет виден на экране - это нормально!)
   - Нажмите Enter

4. **Проверка успешного подключения:**
   - Если всё правильно, вы увидите строку типа:
     ```
     root@server:~#
     ```
   - Это означает, что вы на сервере!

**Если не работает SSH:**
- Установите OpenSSH:
  1. Настройки Windows → Приложения → Дополнительные компоненты
  2. Найдите "Клиент OpenSSH"
  3. Включите его

#### Способ B: PuTTY (проще для начинающих)

1. **Скачайте PuTTY:**
   - Откройте браузер
   - Перейдите на сайт: `https://www.putty.org/`
   - Скачайте и установите PuTTY

2. **Подключитесь:**
   - Откройте PuTTY
   - В поле "Host Name" введите IP адрес вашего сервера
   - Порт оставьте `22`
   - Нажмите "Open"

3. **Войдите:**
   - Введите логин: `root`
   - Нажмите Enter
   - Введите пароль
   - Нажмите Enter

**Готово!** Вы подключены к серверу.

**Как понять, что вы на сервере:**
- Вы увидите строку командную строку с именем сервера
- Обычно выглядит как: `root@имя-сервера:~#`

---

### 📁 ШАГ 3: Найти директорию Supabase

**Зачем:** Нам нужна папка, где установлен Supabase, чтобы настроить MCP endpoint.

**Что делать:**

1. **Вы на сервере через SSH** (см. Шаг 2)

2. **Найдите папку Supabase:**
   ```bash
   find / -name "docker-compose.yml" 2>/dev/null | grep supabase
   ```
   
   **Что это значит:** Команда ищет файл `docker-compose.yml`, который есть в папке Supabase

3. **Пример результата:**
   ```
   /root/supabase/docker-compose.yml
   ```
   
   В этом случае папка Supabase находится в `/root/supabase`

4. **Запишите путь в блокнот:** **Путь к Supabase:** _______________

5. **Перейдите в эту папку:**
   ```bash
   cd /root/supabase  # замените на ваш путь
   ```

6. **Проверьте, что вы в правильной папке:**
   ```bash
   ls
   ```
   
   Должны увидеть файл `docker-compose.yml` и папку `volumes`

**Готово!** Вы в папке Supabase.

**Если не нашли:**
- Проверьте, что Supabase действительно установлен
- Попробуйте поискать вручную:
  ```bash
  ls /root/
  ls /opt/
  ls /home/
  ```

---

### ⚙️ ШАГ 4: Включить MCP endpoint в Supabase

**Зачем:** MCP endpoint встроен в Supabase, но по умолчанию заблокирован. Нужно его включить.

**Что делать:**

1. **Вы на сервере в папке Supabase** (см. Шаг 3)

2. **Откройте файл конфигурации Kong:**
   ```bash
   nano ./volumes/api/kong.yml
   ```
   
   **Что такое nano:** Это простой текстовый редактор прямо в командной строке

3. **Найдите секцию MCP:**
   - Нажмите `Ctrl + W` (поиск)
   - Введите: `mcp`
   - Нажмите Enter

4. **Вы увидите что-то вроде:**
   ```yaml
   - name: mcp
     routes:
       - name: mcp
         strip_path: false
         paths:
           - /mcp
     plugins:
       - name: request-termination
         config:
           status_code: 403
           message: "MCP endpoint is disabled"
   ```

5. **Закомментируйте или удалите плагин `request-termination`:**
   
   Найдёте строки:
   ```yaml
   - name: request-termination
     config:
       status_code: 403
       message: "MCP endpoint is disabled"
   ```
   
   Закомментируйте их (добавьте `#` в начале каждой строки):
   ```yaml
   # - name: request-termination
   #   config:
   #     status_code: 403
   #     message: "MCP endpoint is disabled"
   ```

6. **Добавьте IP ограничение (опционально, но рекомендуется):**
   
   После плагинов добавьте:
   ```yaml
   - name: ip-restriction
     config:
       allow:
         - 172.18.0.1  # Docker bridge IP
   ```

   **Как узнать Docker bridge IP:**
   ```bash
   docker inspect supabase-kong --format '{{range .NetworkSettings.Networks}}{{println .Gateway}}{{end}}' | head -1 | tr -d ' '
   ```

7. **Сохраните файл:**
   - Нажмите `Ctrl + X` (выход)
   - Нажмите `Y` (сохранить изменения)
   - Нажмите `Enter` (подтвердить имя файла)

8. **Перезапустите Kong:**
   ```bash
   docker-compose restart kong
   ```

9. **Проверьте, что MCP endpoint работает:**
   ```bash
   curl http://localhost:8000/mcp/health
   ```
   
   Если видите ответ - всё работает!

**Готово!** MCP endpoint включён.

**Если что-то пошло не так:**
- Проверьте, что файл `kong.yml` сохранён правильно
- Убедитесь, что Kong перезапущен
- Посмотрите логи: `docker-compose logs kong`

---

### 🌐 ШАГ 5: Настроить безопасное подключение с вашего компьютера

**Зачем:** Создать безопасный "туннель" с вашего компьютера на сервер, чтобы Cursor мог подключиться.

**Что делать:**

1. **На вашем компьютере откройте PowerShell** (НЕ на сервере!)

2. **Создайте SSH туннель:**
   ```powershell
   ssh -N -L 3100:localhost:8000 root@ваш-ip-адрес
   ```
   
   **Замените:**
   - `ваш-ip-адрес` на IP адрес вашего сервера
   
   **Пример:**
   ```powershell
   ssh -N -L 3100:localhost:8000 root@123.45.67.89
   ```

3. **Введите пароль** (когда попросит)

4. **Важно:** Окно должно оставаться открытым!
   - Не закрывайте это окно
   - В нём будет работать туннель
   - Минимизируйте окно, если нужно, но не закрывайте

**Что увидите:**
- Окно командной строки с текстом
- Если всё работает - окно будет "висеть" (это нормально!)

**Альтернатива: Использовать готовый скрипт**

1. Найдите файл `start-ssh-tunnel.bat` в папке `mcp-supabase-setup`

2. Откройте его в блокноте и отредактируйте:
   ```
   set REMOTE_HOST=ваш-ip-адрес
   set REMOTE_USER=root
   ```

3. Сохраните и запустите двойным кликом

**Готово!** Туннель работает.

**Если окно закрывается сразу:**
- Возможно, не установлен SSH клиент
- Установите Git для Windows (в него входит SSH)
- Или используйте PowerShell напрямую (см. выше)

---

### 🎯 ШАГ 6: Настроить Cursor

**Зачем:** Сказать Cursor, где находится MCP сервер.

**Что делать:**

1. **Откройте Cursor IDE**

2. **Откройте настройки:**
   - Нажмите `Ctrl + ,` (запятая)
   - Или: File → Preferences → Settings

3. **Найдите раздел MCP:**
   - Вверху окна настроек есть поиск
   - Введите "MCP" или "Model Context Protocol"
   - Найдите раздел "MCP Servers"

4. **Добавьте новый сервер:**
   - Нажмите кнопку "Add Server" или "+" (плюс)
   - Введите имя: `supabase`

5. **Настройте подключение через HTTP:**
   
   - **Тип:** `HTTP` или `HTTP Server`
   - **URL:** `http://localhost:3100/mcp`
   
   **ВАЖНО:** Используйте именно HTTP, НЕ команду npx!

6. **Сохраните настройки** (обычно сохраняется автоматически)

7. **Перезапустите Cursor:**
   - Закройте Cursor полностью
   - Откройте снова

**Если не нашли настройки MCP:**

1. Нажмите `Win + R`
2. Введите: `%APPDATA%\Cursor\User`
3. Нажмите Enter
4. Найдите файл `settings.json`
5. Откройте его в блокноте
6. Добавьте (или измените) следующее:

```json
{
  "mcpServers": {
    "supabase": {
      "type": "http",
      "url": "http://localhost:3100/mcp"
    }
  }
}
```

7. Сохраните файл
8. Перезапустите Cursor

**Готово!** Cursor настроен для работы с MCP сервером.

---

### ✅ ШАГ 7: Проверить, что всё работает

**Зачем:** Убедиться, что всё настроено правильно и работает.

**Что делать:**

1. **Проверьте, что всё запущено:**
   - ✅ SSH туннель работает (окно открыто)
   - ✅ Kong работает на сервере
   - ✅ Cursor перезапущен

2. **Проверьте подключение (опционально):**
   - Откройте PowerShell
   - Выполните: `curl http://localhost:3100/mcp/health`
   - Должен быть какой-то ответ

3. **Попробуйте дать Cursor задачу:**
   
   Откройте чат в Cursor и введите:
   ```
   Покажи список таблиц в базе данных Supabase
   ```
   
   Или:
   ```
   Какие таблицы есть в моей базе данных?
   ```

4. **Что должно произойти:**
   - Cursor подключится к MCP серверу
   - Запросит список таблиц из базы данных
   - Покажет вам результат

5. **Если всё работает - попробуйте создать таблицу:**
   ```
   Создай таблицу test_table с полями:
   - id (целое число, автоинкремент, первичный ключ)
   - name (текст)
   - email (текст, уникальный)
   - created_at (дата и время)
   ```

**Если что-то не работает:**

1. **Проверьте SSH туннель:**
   - Убедитесь, что окно с туннелем открыто
   - Проверьте, что введён правильный IP адрес
   - Попробуйте перезапустить туннель

2. **Проверьте Kong на сервере:**
   - Подключитесь к серверу через SSH
   - Выполните: `docker-compose logs kong`
   - Посмотрите, нет ли ошибок
   - Проверьте: `curl http://localhost:8000/mcp/health`

3. **Проверьте настройки Cursor:**
   - Убедитесь, что URL правильный (`http://localhost:3100/mcp`)
   - Убедитесь, что тип подключения `http` (не `command`)
   - Попробуйте перезапустить Cursor

4. **Если ничего не помогает:**
   - Опишите, что именно не работает
   - Покажите ошибку
   - Я помогу разобраться!

**Готово!** Если всё работает - поздравляю! Теперь вы можете работать с базой данных через Cursor!

---

## Что дальше?

Теперь вы можете:

✅ Просить Cursor показывать таблицы в базе данных
✅ Просить Cursor создавать новые таблицы
✅ Просить Cursor изменять структуру таблиц
✅ Просить Cursor выполнять SQL запросы
✅ Просить Cursor добавлять данные в таблицы

**Примеры запросов для Cursor:**

```
Покажи все таблицы в базе данных
```

```
Создай таблицу users с полями:
- id (целое число, автоинкремент)
- name (текст, не пустое)
- email (текст, уникальный, не пустое)
- password_hash (текст)
- created_at (дата и время, по умолчанию сейчас)
```

```
Добавь поле age в таблицу users
```

```
Выполни SELECT запрос: покажи всех пользователей старше 18 лет
```

```
Создай индекс на поле email в таблице users
```

**Удачи в разработке!** 🚀

---

## Частые проблемы и решения

### Проблема: "Не могу подключиться к серверу по SSH"

**Решение:**
- Проверьте, что IP адрес правильный
- Проверьте, что пароль правильный
- Убедитесь, что сервер включён и доступен в интернете
- Проверьте firewall на сервере (порт 22 должен быть открыт)

### Проблема: "Не могу найти папку Supabase"

**Решение:**
- Убедитесь, что Supabase действительно установлен
- Попробуйте поискать вручную: `ls /root/`, `ls /opt/`, `ls /home/`
- Используйте команду поиска: `find / -name "docker-compose.yml" 2>/dev/null`

### Проблема: "MCP endpoint не работает"

**Решение:**
- Проверьте, что файл `kong.yml` сохранён правильно
- Убедитесь, что Kong перезапущен: `docker-compose restart kong`
- Проверьте логи: `docker-compose logs kong`
- Проверьте доступность: `curl http://localhost:8000/mcp/health`

### Проблема: "Cursor не видит MCP сервер"

**Решение:**
- Убедитесь, что SSH туннель запущен
- Проверьте, что порт 3100 не занят: `curl http://localhost:3100`
- Перезапустите Cursor
- Проверьте настройки MCP в Cursor ещё раз
- Убедитесь, что используете тип подключения `http` (не `command`)

### Проблема: "SSH туннель закрывается сразу"

**Решение:**
- Убедитесь, что SSH клиент установлен (Git для Windows включает SSH)
- Проверьте, что IP адрес и пароль правильные
- Попробуйте подключиться к серверу напрямую через SSH

---

## Шпаргалка команд

### На сервере:

```bash
# Найти папку Supabase
find / -name "docker-compose.yml" 2>/dev/null | grep supabase

# Перейти в папку Supabase
cd /path/to/supabase

# Открыть конфигурацию Kong
nano ./volumes/api/kong.yml

# Перезапустить Kong
docker-compose restart kong

# Проверить, работает ли MCP endpoint
curl http://localhost:8000/mcp/health

# Посмотреть логи Kong
docker-compose logs kong
```

### На локальном компьютере:

```powershell
# Создать SSH туннель
ssh -N -L 3100:localhost:8000 root@ваш-ip-адрес

# Проверить, работает ли SSH туннель
curl http://localhost:3100/mcp/health
```

---

**Если у вас остались вопросы - не стесняйтесь спрашивать!** 😊
