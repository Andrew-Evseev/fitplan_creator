# ⚠️ КРИТИЧЕСКАЯ ПРОБЛЕМА БЕЗОПАСНОСТИ

## Обнаруженные проблемы:

1. **В `lib/core/supabase/supabase_config.dart`:**
   - Хардкожен ANON_KEY
   - Хардкожен URL сервера
   - Закомментированный SERVICE_ROLE_KEY (виден в коде!)

2. **В документации (MD файлах):**
   - SSH пароль сервера
   - Пароль Supabase
   - IP адрес сервера

## Что нужно сделать НЕМЕДЛЕННО:

1. **Ротация всех ключей и паролей:**
   - Сгенерировать новые ANON_KEY и SERVICE_ROLE_KEY в Supabase
   - Изменить SSH пароль на сервере
   - Изменить пароль Supabase

2. **Удалить секреты из истории Git:**
   - Использовать `git filter-branch` или `git filter-repo`
   - Или создать новый репозиторий без истории

3. **Использовать переменные окружения:**
   - Переместить все секреты в `.env` файл
   - Добавить `.env` в `.gitignore`
   - Создать `.env.example` с примерами

4. **Удалить секреты из документации:**
   - Заменить реальные пароли на `YOUR_PASSWORD_HERE`
   - Использовать переменные окружения в примерах
